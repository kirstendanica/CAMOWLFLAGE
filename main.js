// Array for each individual game
let gameCells = document.querySelectorAll('.game-cell');

// Event listeners to 'click' on each game cell
gameCells.forEach(cell => {
    cell.addEventListener('click', function() {
        // TBC (add game logic)
    });
});
